# 🏪 Retail Decision Engine

> Demand Forecasting × Dynamic Pricing × Inventory Optimization — all in one dashboard.

A Streamlit app that combines Deep Learning and Mathematical Optimization models to give grocery and retail managers a weekly decision brief: what price to set, how much demand to expect, and when to reorder.

---

## Live Demo

🔗 [Open App](https://retail-decision-engine-4h8ohlz3u2zz5tvzkubhh2.streamlit.app/)

---

## Features

### 📈 Demand Forecasting (Deep Learning)
- Powered by **Temporal Fusion Transformer (TFT)** (`pytorch-forecasting`), trained on 3 years of daily synthetic sales data.
- Captures complex non-linear relationships between pricing, seasonality, and macro trends.
- Real-time dynamic inference using cached PyTorch models (`.ckpt`) for seamless UX.

### 💰 Dynamic Pricing
- AI-driven demand simulation: Interactive price slider feeds directly into the TFT model to predict future demand dynamically.
- SciPy optimizer calculates the theoretical profit-maximizing price benchmark.
- Hybrid Rendering Visualization: Compares the theoretical price elasticity curve against the AI's 1-point predicted demand and profit.

### 📦 Inventory Optimization
- OR-Tools CP-SAT solver plans optimal order quantities for the next 8 weeks.
- **Fully Integrated Pipeline:** The inventory solver directly uses the dynamic demand forecast generated by the TFT model based on the user's selected price.
- Hard constraints: maximum warehouse capacity, lead time.
- Soft constraints & Objective: Uses slack variables to model safety stock violations and stockouts as penalty costs. The solver minimizes total cost (order fixed cost, holding cost, plus penalties) to ensure mathematical feasibility even in extreme edge cases.

### 🎯 Summary Dashboard
- Integrates all three modules into one actionable view per product.
- Real-time state management: Updates KPIs (Demand, Revenue, Profit impact) instantly based on the price scenarios tested in the individual product tabs.
- Gap analysis: Recommends actionable pricing strategies (Raise/Lower/Keep) by comparing current settings with optimal targets.

---

## Tech Stack

| Module | Technology |
|---|---|
| Deep Learning / Forecasting | `torch`, `lightning`, `pytorch-forecasting` (Temporal Fusion Transformer) |
| Dynamic Pricing Benchmark | `scipy.optimize` minimize_scalar |
| Inventory Optimization | `ortools` CP-SAT solver |
| Visualization | `plotly` |
| UI & State Management | `streamlit` |
| Data Processing | `pandas`, `numpy` |

---

## Project Structure

```text
retail-decision-engine/
├── app.py              # Streamlit UI — tabs, sidebar, dashboard
├── data_generator.py   # Synthetic daily sales data with price elasticity
├── tft_inference.py    # PyTorch TFT model loading and dynamic inference
├── train.py            # PyTorch Lightning training script for TFT
├── forecasting.py      # Legacy/Decomposition tools
├── pricing.py          # Price elasticity + SciPy optimization
├── inventory.py        # OR-Tools CP-SAT inventory planner
├── models/             # Directory containing trained .ckpt model weights
├── .streamlit/         # Theme & optional Streamlit settings (Cloud picks these up)
├── requirements.txt
└── .gitignore
```

---

## Getting Started

### Prerequisites
- Python 3.10+ (Tested on 3.11/3.12)
- pip / Docker

### Installation

```bash
git clone https://github.com/mona2083/retail-decision-engine.git
cd retail-decision-engine
python -m venv .venv
source .venv/bin/activate      # Windows: .venv\Scripts\activate
pip install -r requirements.txt
streamlit run app.py
```

Open [http://localhost:8501](http://localhost:8501) in your browser.

---

## Usage

### Demo Data
1. Click **"🎲 Generate & Run Models"** in the sidebar to generate 3 years of synthetic daily data and run all models.
2. Change the **Random Seed** to generate a different dataset.
3. Select a product tab (🥛 Milk / 🍞 Bread / 🍊 Orange Juice).

### Scenario Simulation (End-to-End Pipeline)
1. In the **Dynamic Pricing** section, adjust the price slider. Watch the AI predict the new demand curve.
2. Go to the **📦 Inventory Optimization** section within the same tab.
3. Click **"🚀 Optimize Order Plan"**. The OR-Tools solver will calculate the order plan based on the *AI's newly predicted demand* from your price setting.
4. Results and recommended actions appear in the **🎯 Summary Dashboard** at the bottom of the page.

---

## How the Models Connect

```text
[User Input] Planned Price ──┐
                             ▼
 Historical Sales ─────►  Temporal Fusion Transformer (TFT)
                             │
                             ▼
                     Predicted Demand (8 weeks)
                             │
            ┌────────────────┴────────────────┐
            ▼                                 ▼
   OR-Tools Inventory                  Profit Calculation
   Optimizer (CP-SAT)                 (vs Theoretical Opt.)
            │                                 │
            └────────────────┬────────────────┘
                             ▼
                    Summary Dashboard
           (Real-time Actions & Gap Analysis)
```

---

## Products & Parameters

| Product | Base Price | Elasticity | Peak Season |
|---|---|---|---|
| 🥛 Milk | $3.99 | -1.8 | Winter |
| 🍞 Bread | $2.49 | -1.4 | November |
| 🍊 Orange Juice | $4.99 | -2.2 | December–January |

---

## Language Support

Toggle between **English** and **日本語** using the language selector in the sidebar.

---

## Deploying (Streamlit Cloud) & TFT weights

If the checkpoint is missing at runtime, the app still starts and uses the **price elasticity model** as a fallback.

**Sidebar shows `Path: /mount/src/retail-decision-engine/models/tft_best_model.ckpt` but “file not found”?**  
That path is correct: Streamlit already uses your app folder as the root. The problem is that **the git commit Cloud cloned does not contain that file** (not a wrong path).

Verify:

1. **Same GitHub repo** — The repository in **Streamlit → App settings → Repository** is the same repo where you see `models/tft_best_model.ckpt` (easy to confuse a **monorepo** with a separate **`retail-decision-engine`** repo).
2. **Branch** — The deployed branch includes the commit that added the `.ckpt`.
3. **Tracked in git** — From that repo/branch, run:  
   `git ls-files models/tft_best_model.ckpt`  
   It must print one line. If it prints nothing, the file was never added to git on that branch.
4. **Git LFS** — If GitHub shows only a small file (pointer), ensure LFS objects are pushed (`git lfs push --all origin`) and the file is not `.gitignore`’d.

5. **Main file path** — Only if the app lives in a subfolder of a larger repo, set e.g. `retail-decision-engine/app.py` so `models/` sits next to `app.py` in the clone.

You can also set **`TFT_MODEL_PATH`** in Streamlit app settings if the checkpoint lives elsewhere.

Locally, run `train.py` (or copy a trained `.ckpt`) into `models/tft_best_model.ckpt`.

### TFT load errors (`NotImplementedError`)

On some Python / PyTorch / torchmetrics combinations, loading a Lightning checkpoint can fail inside **torchmetrics** during `model.to(device)` with `NotImplementedError`. The app catches this and falls back to the elasticity model.

**Mac (Apple Silicon) + MPS:** If the error mentions **`MPS` backend** / `aten::empty.memory_format`, PyTorch tried to use the Metal backend where some ops are missing. The app sets **`PYTORCH_ENABLE_MPS_FALLBACK`**, uses a **`map_location` callback** to force CPU when reading the checkpoint, and calls **`.cpu()`** on the loaded model. Pull the latest `tft_inference.py` and redeploy.

If the sidebar says **missing** (not **error**), the issue is not Python version—it is that the **`.ckpt` is not in the deployed clone**.

---

## Author

**Manami Oyama** — AI Engineer / Product Manager  
🌺 Honolulu, Hawaii  
🔗 [Portfolio](https://mona2083.github.io/portfolio-2026/index.html) | [GitHub](https://github.com/mona2083) | [LinkedIn](https://www.linkedin.com/in/manami-oyama/)